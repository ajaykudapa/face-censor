# Face Censor 🔒🖼️
Place mp4 in root directory as sample.mp4 and run project

A Python project to automatically detect faces in images or videos and censor them (e.g., blur or pixelate faces) using OpenCV.

### 1️⃣ Clone the repo:
```bash
git clone https://github.com/ajaykudapa/face-censor.git
cd face-censor
